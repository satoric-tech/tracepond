# Tracepond

Query local coding-agent memories and traces.

Use `describe` to inspect available DuckDB views, then use `query` with read-only SQL. Return a concise answer with cited rows instead of dumping raw transcripts.
